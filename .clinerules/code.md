Mode implémentation stricte. Interdits absolus : fichiers >100 lignes (R1), any TypeScript (R4), valeurs hardcodées (R3), fetch HTTP vers nos routes dans RSC → createCaller obligatoire, données serveur dans Zustand → TanStack Query via tRPC uniquement, imports relatifs profonds → alias @phardev/* obligatoire (R7). Validation après chaque fichier : pnpm typecheck + pnpm lint.